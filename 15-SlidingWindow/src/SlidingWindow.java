public class SlidingWindow {
    static void main() {
        //code template for sliding window

        /// fixed size window
        /*
        int l = 0;

        for (int r = 0; r < n; r++) {

            // ADD nums[r] / s.charAt(r)
            // update window information


            if (r - l + 1 == k) {

                // window is ready
                // calculate/update answer


                // REMOVE nums[l] / s.charAt(l)
                // update window information

                l++;
            }
        }
         */

        /// variable size - Longest Valid window

//        int l = 0;
//
//        for (int r = 0; r < n; r++) {
//
//            // ADD r
//            // update information
//
//
//            while (/* window is INVALID */) {
//
//                // REMOVE l
//                // update information
//
//                l++;
//            }
//
//            // window is valid
//            ans = Math.max(ans, r - l + 1);
//        }
//
    }
}
